{
  "name": "design-system-tokens",
  "version": "0.0.1",
  "scripts": {
    "update-json": "ts-node tools/generate-tokens.ts",
    "update-scss": "ts-node tools/scss-generator.ts",
    "update-tokens": "npm run update-json && npm run update-scss"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "ts-node": "^10.9.2"
  }
}
